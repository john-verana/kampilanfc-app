
# --- Server ---
server.port=8080

# --- MySQL Database ---
spring.datasource.url=jdbc:mysql://localhost:3306/kampilanfc?useSSL=false&serverTimezone=Asia/Manila&allowPublicKeyRetrieval=true
spring.datasource.username=kampilanfc_user
spring.datasource.password=Kampilan!011

# --- JPA / Hibernate ---
# creates/updates tables automatically during dev
spring.jpa.hibernate.ddl-auto=update   
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Thymeleaf (disable caching in dev) ---
spring.thymeleaf.cache=false

# --- Logging (optional for debugging) ---
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
